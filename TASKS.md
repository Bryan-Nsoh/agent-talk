# Writing Workflow Task

- Collect heuristics from a well-structured reference paper (e.g., a Google research publication) covering section flow, float placement, typography, and narrative devices. Distill these into explicit rules that can be baked into `agents.md` for “writing mode” so the agent applies them by default.
